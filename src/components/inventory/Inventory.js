import React from "react";
import Header from "../Header";

const Inventory = () => {
  return (
    <div>
      <Header></Header>
      <h4>Inventory</h4>
    </div>
  );
};

export default Inventory;